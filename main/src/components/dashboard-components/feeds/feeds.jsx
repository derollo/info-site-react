import React from "react";
import {
    Card,
    CardBody,
    CardTitle,
} from 'reactstrap';

const Feeds = () => {
    return (
        <Card>
            <CardBody>
                <CardTitle>Feeds</CardTitle>
                    <p>This box might not be needed at all, depends on the rest of the content + boxes</p>
            </CardBody>
        </Card>
    );
}

export default Feeds;
